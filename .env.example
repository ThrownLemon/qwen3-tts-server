# Server
HOST=0.0.0.0
PORT=8880
WORKERS=1

# Backend: "official" (default) or "vllm_omni"
TTS_BACKEND=official

# Pre-warm models on startup (slower start, faster first request)
TTS_WARMUP_ON_START=true

# CORS (comma-separated origins, or * for all)
CORS_ORIGINS=*
